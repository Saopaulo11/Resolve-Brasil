/**
 * Границы рассказа о проблеме.
 *
 * Держатся в одном месте, потому что их знают трое: схема проверки, поле
 * формы и счётчик под ним. Разъехавшись, они дают худший из возможных
 * отказов — браузер молча обрезает текст на своём пределе, а сервер
 * ругается на длину, которой человек уже не видит.
 */

/** Короче этого рассказ не даёт понять ни ситуацию, ни что с ней делать. */
export const DESCRIPTION_MIN = 20;

/**
 * Предел сверху. Нужен не для красоты: текст уходит в модель целиком, и
 * без потолка один человек способен вылить туда роман.
 */
export const DESCRIPTION_MAX = 10_000;
